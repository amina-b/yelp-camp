<%- include("partials/header") %>
<div class="row">
        <div class="col-md-3 col-sm-6"> 
            <div class="thumbnail">
                <img src="<%= campground.image %>">
                <div class="caption">
                    <h4><%= campground.name %></h4>
                </div>
                <div>
                    <%=  campground.description %>
                </div>
            </div>
            <a class="btn btn-primary btn-lg" href="/campgrounds/<%=campground._id%>"> More Info</a>
        </div>
</div>
<%- include("partials/footer") %>